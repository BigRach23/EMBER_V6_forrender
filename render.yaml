services:
  - type: web
    name: EMBER_V6
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python firms_server.py"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
    disk:
      name: ember_data_disk
      mountPath: /data
      sizeGB: 1
